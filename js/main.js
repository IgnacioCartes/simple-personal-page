/* globals window */
window.onload = function(event) {

    /* Nothing so far,
    but here's where you would add javascript code to be executed when the page has loaded */
    window.console.log("Ready to do fancy stuff!");
    
};